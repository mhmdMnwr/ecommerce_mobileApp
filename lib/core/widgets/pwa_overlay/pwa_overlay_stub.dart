import 'package:flutter/material.dart';

class PwaOverlayWrapper extends StatelessWidget {
  final Widget child;
  const PwaOverlayWrapper({super.key, required this.child});

  @override
  Widget build(BuildContext context) => child;
}
