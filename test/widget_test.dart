// Placeholder test — will be updated once features are implemented.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: Add widget tests once feature screens are built.
    expect(true, isTrue);
  });
}
